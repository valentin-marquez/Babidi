<section class="hero min-h-screen transform-gpu overflow-hidden">
  <div class="z-10 flex flex-col items-center text-center">
    <h1
      class="mb-5 font-syne text-3xl font-bold text-white drop-shadow-lg md:text-5xl lg:max-w-4xl lg:text-7xl lg:leading-[3.7rem]"
      style="text-wrap: balance;"
    >
      Intercambia con Babidi, vive la magia.
    </h1>
    <p
      class="mx-auto mb-6 max-w-sm font-sora text-xl text-white opacity-75 drop-shadow-xl md:max-w-md md:text-2xl lg:mb-8 lg:max-w-2xl"
    >
      Únete a Babidi y comienza a intercambiar hoy mismo.
    </p>
    <a
      href="signup"
      class="btn-black hero-btn animation-gravity btn btn-md rounded-xl shadow-sm shadow-primary transition duration-300 ease-in-out md:btn-md lg:btn-lg hover:border-2 hover:border-primary hover:text-white"
      >Intercambiar</a
    >
  </div>
</section>

<style>
  @keyframes gradient {
    0% {
      background-position: 0% 50%;
      filter: brightness(1);
    }
    50% {
      background-position: 100% 50%;
      filter: brightness(1.5);
    }
    100% {
      background-position: 0% 50%;
      filter: brightness(1);
    }
  }

  section {
    position: relative;
    overflow: hidden;
  }

  section:before {
    content: "";
    position: absolute;
    z-index: -1;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      180deg,
      theme("colors.secondary") 0%,
      theme("colors.primary") 100%
    );
    -webkit-mask-image: radial-gradient(rgba(0, 0, 0, 0.85), transparent 70%);
    mask-image: radial-gradient(rgba(0, 0, 0, 0.54), transparent 70%);
    animation: gradient 4s ease infinite;
  }
</style>
